apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iamgenii.fullname" . }}-test-connection"
  labels:
    {{- include "iamgenii.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iamgenii.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
